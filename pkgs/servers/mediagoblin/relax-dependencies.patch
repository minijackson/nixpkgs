diff --git a/setup.py b/setup.py
index 897d1d2a..4ba1846f 100644
--- a/setup.py
+++ b/setup.py
@@ -45,17 +45,17 @@ install_requires = [
     'py-bcrypt',
     'pytest>=2.3.1',
     'pytest-xdist',
-    'werkzeug>=0.7,<1.0.0',
+    'werkzeug',
     # Celery 4.3.0 drops the "sqlite" transport alias making our tests fail.
-    'celery>=3.0,<4.3.0',
+    'celery>=3.0',
     # Jinja2 3.0.0 uses f-strings (Python 3.7 and above) but `pip install` on
     # Debian 9 doesn't seem to respect Jinja2's 'python_requires=">=3.6"' line.
-    'jinja2<3.0.0',
+    'jinja2',
     'Babel>=1.3',
     'WebTest>=2.0.18',
     'ConfigObj',
     'Markdown',
-    'sqlalchemy<1.4.0',
+    'sqlalchemy',
     'itsdangerous',
     'pytz',
     'sphinx',
